# frontend/

React + Vite + TypeScript PWA. Intentionally empty at scaffold time: the frontend starts
after the first real API routes exist, so its data layer can be generated from the FastAPI
OpenAPI spec (`openapi-typescript`) instead of hand-written against guesses.

See `research/tech-stack.md` §1 for the decided stack and conventions.
