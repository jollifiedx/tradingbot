# Copy to backend/.env for local dev. NAMES ONLY here — real values never enter git.

# Webull OpenAPI (paper credentials for dev; live keys exist only in Railway live env)
WEBULL_APP_KEY=
WEBULL_APP_SECRET=
WEBULL_ENV=paper
WEBULL_PAPER_API_ENDPOINT=

# Anthropic API (research pipeline)
ANTHROPIC_API_KEY=

# Supabase
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=
