# Broker Integrator Memory

- [Webull SDK quirks](webull_sdk_quirks.md) — import name, hidden network I/O, credential-leaking logging, timeout masking, enum serialization; USE account_v2 (v1 paths 404), confirmed sandbox balance field names (v2.0.14).
- [Webull paper endpoint — RESOLVED](webull_paper_endpoint_open_question.md) — sandbox host api.sandbox.webull.com is correct; 404 was a wrong SDK method (v1 vs account_v2), not the host. account_v2 returns 200.
